mutation {
    admin {
        jahia {
            configuration(pid:"org.owasp.csrfguard.cfg", updateOnly: true) {
                tokenPerPage: value(name:"org.owasp.csrfguard.TokenPerPage", value:"true")
            }
        }
    }
}
